#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../grunt-crx/bin/grunt-crx" "$@"
  ret=$?
else 
  node  "$basedir/../grunt-crx/bin/grunt-crx" "$@"
  ret=$?
fi
exit $ret
